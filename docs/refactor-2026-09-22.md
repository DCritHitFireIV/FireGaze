# FireGaze 重构准备：对齐 AtmoOmen 规范

- 日期：2026-09-22
- 状态：**已批准，执行中**
- 执行位置：**新私有仓库 `DCritHitFireIV/FireGaze-Refactor`**（原 `DCritHitFireIV/FireGaze` 不动）
- 目标：让 FireGaze 的工程规范 / 代码风格 / 注释风格贴近 `DailyRoutines`（ModulesPublic）、`UntarnishedHeart`、`OmenTools` 三个仓库的共同规范
- 方法：Fowler《重构》式**行为保持、小步推进**（见 §1）

---

## 1. 方法与依据

**使用的重构 skill（GitHub 上找的，本地没有）**

| Skill | 用途 |
|---|---|
| `ciembor/agent-rules-books` → `refactoring`（MIT, 2839★） | 重构总纪律：行为保持、小步可构建、机械改动与行为改动分离、只重构真实摩擦 |
| `ciembor/agent-rules-books` → `working-effectively-with-legacy-code`（Feathers） | FireGaze 大部分逻辑无自动化测试 → 小步 + 特征化 + 接缝先行 |

**规范来源（已逐项取证）**

- `.editorconfig`：`UntarnishedHeart` 与 `DailyRoutines.ModulesPublic` 两份**逐字节完全相同**（`diff` 实证）——本仓库采用同一份。
- 分析器：`DailyRoutines.CodeAnalysis`（**MIT 许可**，nuget 2.6.0）——DR0001（用 `nint`）、DR0002（禁下划线开头）、DR0008（配置类禁 readonly）、DR0009（缩写大小写一致），严重级 Error。
- 实测：给 FireGaze 挂上该分析器后，**报错全部是 DR0009，共 38 个声明点**；DR0001/0002/0008 零命中。
- 排版签名：多行参数时 `(` 独占一行 + 参数列对齐（ModulesPublic 3404 处 / UTH 611 / OmenTools 951；FireGaze 0）。
- `this.` 前缀：AtmoOmen 系几乎不用，FireGaze **1568 处**。
- XML 注释：AtmoOmen 系以块式为主（OmenTools 1438 块 / 32 单行；UTH 38 块 / 0 单行），正文以 `///` + 4 空格缩进；FireGaze 293 单行 / 57 块。

**基线（重构前安全网，2026-09-22 实测）**

`C:\Users\Fire\fgtest` 对 1.2.0.24 产物**全部 12 组断言通过**：清单 / 关键类型 8 个 / 零 Harmony / 归一化 6 例 / 无自递归 / 词表往返 1749 条 / 贡献写盘 / 词表版本号 / 表格列数 / 历史整理 / 截断算式 / 中日判定。
→ 每个阶段结束都必须跑通这套断言（用新仓库产物路径）。

---

## 2. 差距清单

| 维度 | FireGaze 现状 | AtmoOmen 规范 | 动作 | 风险 |
|---|---|---|---|---|
| `.editorconfig` | 没有 | Allman / `var` / 对齐 / 修饰符 private 在前 / 表达式体 | ✅ Phase 1 已移植 | 低 |
| 分析器 | 无 | DailyRoutines.CodeAnalysis（MIT） | Phase 2 挂上 | 低 |
| 缩写大小写 | `Url` 188 / `Utc` 48 / `Id` 20 / `Json` 48 / `Http` 36 / `Html` 2（含使用处与 BCL 名） | DR0009：全大写或全小写 | **38 个声明点**改名 | 中（序列化契约） |
| `this.` 前缀 | 1568 处 | 不用 | 机械清理（Phase 3a） | 低-中 |
| 多行参数签名 | 0 处 `(` 独占行 | `(` 独占行 + 列对齐 | **仅新代码采用**（存量不动） | 低 |
| 初始化器对齐 | 不对齐 | 列对齐 | 新代码手写；存量不动（无工具） | 低 |
| XML 注释 | 293 单行 `<summary>` | 块式 + 4 空格正文缩进 | 脚本化转换（Phase 4） | 低 |
| 大文件 | `ContributeWindow.cs` 2775 / `RepoAuditTab.cs` 2107 | partial 按子域拆 | **以后**（Phase 5 暂缓） | — |
| 配置迁移 | 构造函数内联 v2/v3 | 单步迁移器 + 注册表 | **以后**（Phase 5 暂缓） | — |
| 行尾 / BOM | LF、无 BOM | 同 | 已符合 | — |
| file-scoped namespace | 100% | 100% | 已符合 | — |
| 常量命名 | PascalCase | 混用 | 保持不动 | — |

**序列化契约分析（已实测）**

| 位置 | 序列化器 | 旧键能否读回 | 处理（已定） |
|---|---|---|---|
| `Configuration` + `UndoEntry`（`Url`、`TimeUtc`、`PushUrl`、`LastTableUpdateCheckUtc`、`LastTableUpdateUtc`、`LastScanUtc`） | Newtonsoft（Dalamud 配置） | **能**（实测大小写不敏感） | 接受「读兼容、写新键」，不加注解 |
| `IconCache` 索引（`Entry.Url`、`SavedUtc`） | STJ **默认选项（大小写敏感）** | **不能** | 读取处加 `PropertyNameCaseInsensitive = true`（必做） |
| 贡献导出 `contributions.json` / 历史（`ContributionRecord.Id`） | STJ（已开大小写不敏感） | 能 | **`[JsonPropertyName("Id")]` 钉旧键**（`scripts/import_contributions.py` 按 `"Id"` 读，脚本零改动） |
| `repo-backup-*.json`（匿名类型 `x.Url`） | STJ | — | 接受新键 `URL`（同步改 note 文案） |
| `RepoScanner` 的 `CachedOutcome`/`FetchResult`/`UrlProbe` | 无（纯内存） | — | 随便改 |
| `TranslationIndex` 的 `RepositoryUrl`/`IconUrl` | 无（运行时反射构建） | — | 随便改 |

**注意（反射字符串不得动）**：`"InstalledFromUrl"`、`"IconUrl"`、`"PluginMasterUrl"` 是 Dalamud / 仓库 JSON 的真实属性名，改名脚本必须避开字符串字面量。

---

## 3. 阶段计划（每阶段一个本地提交）

### Phase 1 — 工程规范层 ✅

1. 新增 `.editorconfig`：采用 UTH / ModulesPublic 同一份（逐字节相同）。
2. `.gitattributes` 不动（FireGaze 的 `* text=auto eol=lf` 更严）。
3. 验证：`dotnet build -c Release` 正常。

### Phase 2 — 命名规范化（38 处 DR0009）

**38 个声明点完整清单（分析器实测输出）：**

| # | 文件:行 | 现名 | 新名 | 备注 |
|---|---|---|---|---|
| 1 | Configuration.cs:21 | `UndoEntry.Url` | `URL` | 配置序列化，读兼容 |
| 2 | Configuration.cs:34 | `TimeUtc` | `TimeUTC` | 同上 |
| 3 | Configuration.cs:77 | `LastTableUpdateCheckUtc` | `LastTableUpdateCheckUTC` | 同上 |
| 4 | Configuration.cs:80 | `LastTableUpdateUtc` | `LastTableUpdateUTC` | 同上 |
| 5 | Configuration.cs:99 | `PushUrl` | `PushURL` | 同上 |
| 6 | Configuration.cs:128 | `LastScanUtc` | `LastScanUTC` | 同上 |
| 7 | DalamudRepos.cs:12 | `Url` | `URL` | 内部模型 |
| 8 | DalamudRepos.cs:41 | `repoItemUrlProp` | `repoItemURLProp` | 私有反射字段 |
| 9 | IconCache.cs:22 | `Entry.Url` | `URL` | **icons.json 补读选项** |
| 10 | IconCache.cs:26 | `SavedUtc` | `SavedUTC` | 同上 |
| 11 | InstalledPluginsIndex.cs:17 | `RepositoryUrl` | `RepositoryURL` | 运行时索引 |
| 12 | InstalledPluginsIndex.cs:20 | `IconUrl` | `IconURL` | 同上 |
| 13 | InstalledPluginsIndex.cs:126 | `repositoryUrl` | `repositoryURL` | 局部变量 |
| 14 | InstalledPluginsIndex.cs:208 | 参数 `repositoryUrl` | `repositoryURL` | — |
| 15 | InstalledPluginsIndex.cs:212 | 参数 `normalizedUrl` | `normalizedURL` | — |
| 16 | InstalledPluginsIndex.cs:234 | `NormalizeRepositoryUrl` | `NormalizeRepositoryURL` | **fgtest 反射调用同步** |
| 17 | RepoScanner.cs:21 | `Url` | `URL` | 纯内存 |
| 18 | RepoScanner.cs:24 | `NormalizedUrl` | `NormalizedURL` | 同上 |
| 19 | RepoScanner.cs:35 | `HttpStatus` | `HTTPStatus` | 同上 |
| 20 | RepoScanner.cs:54 | `CheckedUtc` | `CheckedUTC` | 同上 |
| 21 | RepoScanner.cs:111 | record 位参 `HttpStatus` | `HTTPStatus` | 同上 |
| 22 | RepoScanner.cs:119 | record 位参 `Url` | `URL` | 同上 |
| 23 | RepoScanner.cs:149 | `ProbeUrlAsync` | `ProbeURLAsync` | 参数 `url` 小写合规 |
| 24 | ContributionRules.cs:29 | `Html` | `HTML` | 私有 Regex 字段 |
| 25 | ContributionsStore.cs:12 | `ContributionRecord.Id` | `ID` | **加 `[JsonPropertyName("Id")]`** |
| 26 | ContributionsStore.cs:82 | `RepoUrl` | `RepoURL` | const |
| 27 | ContributionsStore.cs:84 | `JsonOptions` | `JSONOptions` | — |
| 28 | ContributionsStore.cs:345 | `BuildJson()` | `BuildJSON()` | — |
| 29 | TranslationIndex.cs:16 | `RepositoryUrl` | `RepositoryURL` | 运行时 |
| 30 | TranslationIndex.cs:60 | `IconUrl` | `IconURL` | 同上 |
| 31 | TranslationIndex.cs:228 | `repoUrl` | `repoURL` | 局部变量 |
| 32 | TranslationIndex.cs:294 | 参数 `repoUrl` | `repoURL` | — |
| 33 | TranslationIndex.cs:318 | `iconUrl` | `iconURL` | — |
| 34 | TranslationTable.cs:48 | `RemoteUrl` | `RemoteURL` | const |
| 35 | TranslationTable.cs:52 | `JsonOptions` | `JSONOptions` | — |
| 36 | ContributeWindow.cs:134 | `Url` | `URL` | UI 模型 |
| 37 | ContributeWindow.cs:921 | `canAddUrl` | `canAddURL` | 局部变量 |
| 38 | InstallerListScroll.cs:38 | `ListChildId` | `ListChildID` | const |

步骤：① 脚本改名（跳过字符串/注释，保护反射字符串）→ ② 序列化处理 → ③ fgtest 同步 → ④ 挂分析器（0 诊断）→ ⑤ build + fgtest + 旧配置回归。

### Phase 3 — 机械排版

- **3a `this.` 清理（1568 处）**：`.editorconfig` 增加 `dotnet_style_qualification_for_* = false`（临时 `:warning`），用 `dotnet format` 的 IDE0003 修复批量应用；核对 diff 只含 `this.` 删除；随后把规则降为 `:suggestion`（保持与 AtmoOmen 一致的“不强制”）。
- **3b 多行签名**：仅新代码采用，存量不动。
- **3c 初始化器对齐**：手写约定，存量不动。

### Phase 4 — 注释风格

- 293 个单行 `/// <summary>X</summary>` → 块式（`///` + 4 空格正文缩进）；现有块式正文缩进统一；`<param>`/`<returns>` 保持单行。
- 脚本化（只动注释），build + fgtest 验证。

### Phase 5 — 结构（暂缓，以后）

- `ContributeWindow` / `RepoAuditTab` partial 拆分；配置迁移抽 `ConfigurationMigrator` + 单步迁移器。

### 收尾

- 版本号 → `1.2.0.25`；每阶段本地提交；**不做任何推送发布动作**（本仓库是私有实验仓库，只推自己的 main）。

---

## 4. 明确不做

- 不动原仓库 `C:\everyone\FireGaze`（保持原样）。
- 不动 `README.md` / 插件简介 / `pluginmaster.json` 文案。
- 不改功能性行为；不引入 Harmony / 钩子。
- 不改 `translations.json` 格式、不动 `scripts/*.py` 输入契约。
- 不做投机抽象。

---

## 5. 决策（已锁定，2026-09-22 用户批准）

| # | 决策点 | 结论 |
|---|---|---|
| ① | `this.` 清理 | **做**（dotnet format IDE0003） |
| ② | 多行签名风格 | **只对新代码**，存量不动 |
| ③ | 序列化旧键 | **接受读兼容写新键**（贡献导出 `Id` 除外，钉键保脚本） |
| ④ | 大文件拆分 / 迁移器抽取 | **以后** |
| ⑤ | 分析器包纳入 | **纳入** |

---

## 6. 执行记录（2026-09-22）

| 阶段 | 内容 | 提交 |
|---|---|---|
| 建仓 | 私有仓库 `DCritHitFireIV/FireGaze-Refactor`（从公开 FireGaze 完整克隆；origin→新仓、upstream→原仓；Actions 已关闭） | — |
| Phase 1 | `.editorconfig` 采用 UTH/ModulesPublic 同一份；重构计划入库 | 09767a5 |
| Phase 2 | 38 处 DR0009 改名 + 序列化契约处理 + 挂分析器（build 0 诊断） | 7bce6ff |
| Phase 3a | `this.` 1568 → 15（dotnet format IDE0003；余下为构造函数必要消歧） | 119be6e |
| Phase 4 | XML 注释块式化（293 个单行 summary → 块式） | 210c43e |
| 收尾 | 版本号 → 1.2.0.25 | 56c15c3 |
| Phase 5b | 配置迁移抽单步迁移器（Internal/Configuration）；全部落盘属性显式 JsonProperty/JsonPropertyName，旧键名钉死（老用户设置双向兼容） | cdac484 |
| Phase 5a | 大文件拆 partial：ContributeWindow → 主文件 + 7 个，RepoAuditTab → 主文件 + 5 个（成员不丢不重：62/62、28/28） | 43f2232 |
| 校验修复 | 注册 `UiBuilder.OpenMainUi`（主入口回调）+ manifest 补 Tags/CategoryTags（csproj `<Tags>`；卫月校验两项清零） | 0b2d066 |

### 偏离/决定记录（Fowler：机械改动与行为改动分离）

- **`UrlProbe` 类型一并改名 `URLProbe`**（分析器不检查类型名；它与本次 Url 家族同源）；**`UiHelpers` 类型保持不动**（与本次缩写无关，留待以后）。
- **`repo-backup-*.json` 的键从 `Url` 变为 `URL`**（决策③接受的落盘键变化；note 文案同步更新）。
- **`ContributionRecord` 的 `Id` 键保持不变**：C# 名 `ID` + `[JsonPropertyName("Id")]`；导出用匿名投影显式写 `Id = x.ID`——Python 脚本零改动。
- **`IconCache` 读取补 `PropertyNameCaseInsensitive = true`**：旧 `icons.json` 的 `Url`/`SavedUtc` 键仍能命中（已加 fgtest 断言）。
- **`.editorconfig` 追加 `dotnet_style_qualification_for_* = false:warning`**（UTH/ModulesPublic 没有这条；用于清理存量 + 约束新代码）。
- **fgtest 副本**：`C:\Users\Fire\fgtest-refactor`（指向新仓库 DLL；原 `C:\Users\Fire\fgtest` 未动）。新增 3 条序列化兼容断言，现共 15 组断言全绿。

### 验证证据（2026-09-22）

- `dotnet build -c Release`：0 警告 / 0 错误（含 DailyRoutines.CodeAnalysis 2.6.0）。
- `fgtest-refactor`：**18/18** 断言通过（含旧配置键读 / 新版本写盘旧键名 / 迁移链 / 旧 icons.json 读 / 图标写盘旧键名 / 导出 Id 键）。
- 改名自检：非注释改动均等价于「去掉 `this.`」或「改名」；字符串字面量与反射名逐条核对未动。
- 拆分自检：两窗口拆分前后方法集合一致（ContributeWindow 62、RepoAuditTab 28），无丢失/重复。

## 7. 尚未做（等用户实机验证后再定）

- **实机验证**（用户）：把新仓库构建出的 DLL 挂到 Dalamud 开发插件位置跑一轮。
- `RepoAuditTab.Draw()` 仍是 553 行的长函数（本次只做了 partial 拆分；函数体拆分成多个 Draw* 小节属于后续可选事项）。
- `UiHelpers` 等类型名缩写：未纳入。
